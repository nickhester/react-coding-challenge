import React from 'react';
import MessageDisplay from './MessageDisplay';

export default class MessageDisplayContainer extends React.Component {
    

    render() {
        return (
            <MessageDisplay />
        )
    }
}